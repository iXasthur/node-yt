extends layout

block content
    div(class='horizontal-stack')
        h1 Welcome to node-yt!
        h1
            a(href='/upload') Upload
    h2 Videos:

    each v in video_list
        h4
            a(href='/watch?id=' + v.id) #{v.name}
    else
        p Nothing available right now
